const Helper = codecept_helper;

class Hooks extends Helper {
  /**
   * before all tests
   */
  _init() {

  }

  /**
   * before a test
   */
  _before() {
  }

  /**
   * after a test
   */
  _after() {
  }
}

module.exports = Hooks;
