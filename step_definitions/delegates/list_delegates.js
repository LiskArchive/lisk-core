When('I request for delegates list with {string}', (params) => {
  return 'pending';
});

When('I request for forging delegates list with {string}', (params) => {
  return 'pending';
});

When('I request for forging delegates statistics with {string}', (params) => {
  return 'pending';
});

Then('I should get delegates according to params', () => {
  return 'pending';
});

Then('I should get forging delegate statistics', () => {
  return 'pending';
});
